@* jQuery validation scripts are already loaded globally in _Layout.cshtml via CDN,
   so this partial is intentionally left empty. It exists so that
   @section Scripts { <partial name="_ValidationScriptsPartial" /> } in views doesn't error out. *@
